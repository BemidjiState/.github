name: "CD: Deploy Prerelease to Staging (Auto)"

# =============================================================================
# Automatic prerelease deployment workflow for zip-delivery packages.
# Copy this file to .github/workflows/deploy-zip-auto.yml in the target repo
# and follow the configuration steps below.
#
# Triggers on prerelease published targeting 'release' branch → staging
#
# For stable release → production, copy deploy-zip-stable-auto.yml instead.
#
# HOW TO CONFIGURE THIS FILE:
# ----------------------------
# 1. Uncomment the job blocks for the environments this package deploys to.
# 2. Set the environment: value in each job to match the location environment
#    name configured for this package in /data/hq/config.json exactly.
# 3. Remove job blocks for environments that do not apply to this package.
#
# KEEP IN SYNC:
# The environment names used here must match:
#   - The options list in deploy-zip-manual.yml and rollback.yml
#   - The HQ_ENVIRONMENTS repository variable
#   - The location environment names in /data/hq/config.json
# The check endpoint validates this on every run.
# =============================================================================

on:
  release:
    types:
      - published

jobs:

  # ---------------------------------------------------------------------------
  # Prerelease published targeting 'release' branch → bsu-staging
  # Uncomment and set environment: to the correct staging environment name.
  # Both conditions are required — prerelease flag AND branch target — to
  # prevent accidental deploys from manually created releases on wrong branches.
  # ---------------------------------------------------------------------------

  # deploy-bsu-staging:
  #   if: >-
  #     github.event.release.prerelease == true &&
  #     github.event.release.target_commitish == 'release'
  #   uses: BemidjiState/.github/.github/workflows/hq-deploy-zip.yml@1.0.11
  #   with:
  #     package:           ${{ vars.HQ_PACKAGE }}
  #     environment:       bsu-staging
  #     ref:               ${{ github.event.release.tag_name }}
  #     yaml_environments: ${{ vars.HQ_ENVIRONMENTS }}
  #     asset_id:          ${{ github.event.release.assets[0].id }}
  #     poll_for_result:   false
  #   secrets:
  #     hmac_secret:       ${{ secrets.ORG__HQ_HMAC }}

  # ---------------------------------------------------------------------------
  # Prerelease published targeting 'release' branch → ntc-staging
  # Remove this block if this package does not deploy to NTC.
  # ---------------------------------------------------------------------------

  # deploy-ntc-staging:
  #   if: >-
  #     github.event.release.prerelease == true &&
  #     github.event.release.target_commitish == 'release'
  #   uses: BemidjiState/.github/.github/workflows/hq-deploy-zip.yml@1.0.11
  #   with:
  #     package:           ${{ vars.HQ_PACKAGE }}
  #     environment:       ntc-staging
  #     ref:               ${{ github.event.release.tag_name }}
  #     yaml_environments: ${{ vars.HQ_ENVIRONMENTS }}
  #     asset_id:          ${{ github.event.release.assets[0].id }}
  #     poll_for_result:   false
  #   secrets:
  #     hmac_secret:       ${{ secrets.ORG__HQ_HMAC }}

